# 01 — 作者识别与屏蔽/信任核心流程

Type: task
Status: done
Priority: P0

**Blocked by:** 无（已交付，commit 063c18e）

从回答卡片提取作者，建立作者规则数据层，并把规则检查插到判定流程最前端：已屏蔽 → 整卡占位；已信任 → 一轮不跑。用户端到端行为：在问题页点开任意角标面板，即可屏蔽/信任该作者，全页该作者卡片即时生效。

**验收：**

- [ ] 回答卡片可提取作者（`zhihu.com/people/<token>` 为主键，昵称用于显示）；匿名回答识别为"不可操作"
- [ ] 角标面板出现「屏蔽该作者」「信任该作者」按钮（匿名卡不显示）
- [ ] 屏蔽后：卡片立即替换为占位条「已屏蔽作者 X」+ [查看][取消屏蔽]，不产生任何提取与判定调用
- [ ] [查看] 临时展开正文；页面重分析后仍保持屏蔽
- [ ] 信任后：该作者卡片一轮不跑、零角标，仅低调灰色小签「已信任」，点击可取消信任 / 改为屏蔽
- [ ] 屏蔽/信任后，本页该作者全部卡片（含已分析的）即时应用规则
- [ ] 作者级规则优先于卡片覆盖与引擎判定
- [ ] 规则跨页面持久化（刷新、重开浏览器保留）

## Comments

- 设计来源：`.scratch/author-rules/spec.md` §3（grill 轮次 Q2–Q8）
- 实现注意：作者规则检查位于提取文本之前；存储写入后经既有 storage 监听跨页面生效

## 交付记录（2026-08）

- 全部验收项通过，浏览器实测：
  - 屏蔽：占位条「已屏蔽作者 X」+ [查看][取消屏蔽]，同一节点零重建（12s 渐进渲染窗口内 0 次 zys 节点增删）
  - 信任：灰色小签「已信任」→ 小面板（取消信任 / 屏蔽该作者），互斥切换正常
  - 优先级：作者 > 卡片覆盖 > 引擎（覆盖「认为人工」后屏蔽 → 占位；取消屏蔽 → 覆盖角标保留）
  - 跨页面持久化（刷新保留）；跳过卡面板同样可操作作者；匿名/无作者卡不显示作者按钮
- 已知边界：首页折叠预览卡（is-collapsed）无 AuthorInfo/people 链接，作者不可识别 → 不参与作者规则；
  展开「阅读全文」渲染完整 AuthorInfo 后自动适用（正文变化重分析路径）。符合"无稳定身份不可操作"语义。
