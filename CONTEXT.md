# CONTEXT — 知乎照妖镜（暂定名）

知乎 AI 答案检测 Chrome 扩展（MV3）的领域词汇。只含词汇表，不含实现细节；由 grilling 会话逐步固化。

## Language（术语表）

**判定分数 (verdict score)**:
对一条回答"由人类撰写"的可信度估计，取值 0–100：100 几乎确定人工，0 几乎确定 AI。采用扣分制，由引擎产出，可被用户覆盖。
_Avoid_: AI 置信度、可疑度

**阈值 (threshold)**:
把判定分数映射为离散等级的配置值，默认两档：≤40 疑似 AI，≤10 确定 AI（分数越低越可疑）。阈值可配置，驱动处置动作。
_Avoid_: 分数线、档位线

**处置动作 (disposition)**:
针对某个判定等级采取的界面行为，如标记提醒、折叠、过滤。MVP 只实现"标记"，折叠/过滤为后续迭代。
_Avoid_: 操作、行为

**引擎 (engine)**:
产出判定分数的两级后端：**初审（规则引擎）**命中 AI 创作痕迹即扣分，输出规则分与命中清单；**二审（云端校验器）**仅在规则分落入模糊带（20–80）时介入，调用 OpenAI 兼容接口的远程 LLM（默认 DeepSeek），分析内容将外发至该服务商。本地模型方案已评估后排除。
_Avoid_: 后端、检测器

**AI 创作痕迹 (AI trace)**:
初审扣分的依据：可列举、可解释的 AI 常见创作特征（措辞套话、工整结构、无个人化细节等）。命中一条即扣对应分值，命中清单用于"点击看理由"。
_Avoid_: AI 特征、扣分项

**输入窗口 (input window)**:
被分析的回答正文片段：全文（折叠长文亦在 DOM 内）与 2000 字上限取较小者（可配置），不含标题，图/引用开头跳至首个文本段；"只看开头一两段"为可选模式。
_Avoid_: 采样片段、文本窗口

**覆盖 (override)**:
用户手动纠正引擎判定结果的记录，优先级高于引擎判定，以回答 ID 为键，持久化于扩展本地存储（chrome.storage.local）。
_Avoid_: 纠正、手动标记

**触发 (trigger)**:
分析发生的时机：页面加载后分析已渲染回答，滚动时分析新出现回答，并提供手动"重新分析"入口。
_Avoid_: 时机、调度
